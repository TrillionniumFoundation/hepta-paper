# Exact-subject invalidation

A changed head, tree, merge object, workflow definition, run attempt, job, step, artifact, policy snapshot or review decision invalidates the retained qualification until it is freshly derived and independently reviewed.
