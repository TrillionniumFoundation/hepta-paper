# Node/Rust command gap closure ledger

> Generated from `docs/migration/node-rust-command-map.v1.json` and the live command registry. Unmapped and partially implemented commands remain open. This ledger does not grant parity, production activation, or Node retirement.

- Source of truth: `docs/migration/node-rust-command-map.v1.json` + live command registry
- Total command routes: **57**
- Unmapped commands: **0**
- Partial source candidates: **57**
- Independently accepted parity rows: **0**
- Open command gaps: **57**
- Accepted parity: `false`
- Production activation: `false`
- Node retirement: `false`

| Route | Source mapping | Node entrypoint | Rust candidate | Category | Current gap | Required closure evidence |
|---|---|---|---|---|---|---|
| `maintenance/autonomous-online-schema-transition` | `partial_local_source` | `node` `paper-core/bin/autonomous-research-online-schema-transition.mjs` | `build_schema_transition_plan_v1 / normalize_schema_maintenance_v1 / resume_schema_normalization_v1 / install_schema_maintenance_v1 / resume_schema_installation_v1 / finalize_schema_transition_v1 / observe_schema_transition_v1` | external authority / target host | Complete CLI modes, target-configuration restart, durable checkpoint/final-receipt publication and missing-history recovery remain open. The offline authority-journal image/archive does not supply live source CAS, authenticated old-process stop/restart exclusion, installed key custody or migration authority. Manager/peer observations do not establish continuous identity of every handler. Complete installed nine-role native provenance, public writable composition, cross-version recovery and independent target-host migration/canary/rollback/Node-retirement acceptance remain required. Implemented source and exact regression owners are the callChain/rustSources/testCases entries; current contracts are docs/modules/ONLINE_SCHEMA_EXECUTION_HANDOFF.md and docs/modules/LOCAL_STATE_AUTHORITY_HANDOFF.md. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `maintenance/autonomous-state-partial-root-maintenance` | `partial_local_source` | `node` `paper-core/bin/autonomous-research-state-partial-root-maintenance.mjs` | `hepta-paper-rust autonomous-state-partial-root-maintenance --action plan\|execute [options]` | full Rust business call chain | Rust now implements the historical manifest-bound 5+5 offline repair: read-only exact plan identity; all-five existing SQLite EXCLUSIVE locks; rescue copy plus restore verification; construction of the five missing business databases through the canonical native schema owner; narrowly scoped supervisor-journal repair; no-clobber publication; post-repair inventory/business-state verification; and durable terminal reconciliation. Real Node-built 5+5 fixtures prove success without Node in the execution PATH, target-appearance refusal, pre-commit SIGKILL rollback followed by fresh-fence retry, post-commit recovery, and exact terminal replay. The route remains a partial compatibility candidate because the incumbent optional policy/provider CLI fields are not all accepted by Rust, online schema transition/installed writable-owner activation is intentionally separate, and target-host/independent operational acceptance remains open. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `maintenance/autonomous-state-provision` | `partial_local_source` | `node` `paper-core/bin/autonomous-research-state-provision.mjs` | `hepta-paper-rust autonomous-state-provision --action plan\|execute [options] [--genesis-inputs ABSOLUTE_JSON --genesis-inputs-sha256 SHA256]; Rust-only recovery profile: hepta-paper-rust autonomous-state-provision --recover-staging ABSOLUTE_REQUEST_JSON; published-root recovery: hepta-paper-rust autonomous-state-provision --recover-publication ABSOLUTE_REQUEST_JSON` | full Rust business call chain | Pinned external genesis now drives actual ten-database native construction, original 25 native migrations, handoff migrations and original-constructor schema templates, genuine metadata/genesis and paired handoff identities. The current profile validates retained inputs, positive budgets and explicit execution confirmation, then publishes through private staging, retained descriptors, same-parent owner locking, NOREPLACE and file/directory fsync. No-genesis diagnostics remain blocked. Existing --recover-staging selects bounded exact byte/inode observations and preserves unpublished bytes by explicit quarantine, with no adoption or deletion. The new Rust-only --recover-publication profile accepts a separately selected prepared-receipt digest, verifies its exact closed non-authorizing shape, source schema bundle/manifest and all ten unchanged database byte hashes, and selects an inode-bound recovery plan. It reuses the publisher terminal derivation, exclusively creates only a missing terminal receipt, verifies final names and bytes, and returns the same terminal result on explicit replay. It does not open or modify SQLite, overwrite an existing conflicting/partial terminal, repair changed state, rerun genesis, acquire a writer or claim live authority. Whole-number float spelling is preserved so recovered terminal bytes match the original. Genuine ten-database CLI tests run without Node in the child PATH; newer SQL state, races, substituted files and actual SIGKILL boundaries remain covered. Root-owned genesis, other incumbent policy/provider flags, full partial-root repair, general published-state maintenance, installed V2 writable-owner composition, host qualification and full command parity remain open. Local recovery only observes the selected historical initialization; an input digest is not current authorization. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `maintenance/command-surface-sync` | `partial_local_source` | `node` `paper-core/bin/command-surface.mjs` `--write-package` | `hepta-paper-rust command-surface ROOT [--write-package\|--check-package\|--npm-aliases\|--help-artifact\|--ci-matrix]` | local tooling | Rust now owns deterministic package-script synchronization with byte-compatible Node JSON rewrite and ordered inspection output for check/write, including JavaScript Object.keys/Object.entries coercion for malformed array/string scripts values, UTF-16 code-unit key ordering and surrogate-safe rewrite output; exact deterministic --npm-aliases, --help-artifact, default classify, and --ci-matrix projections come from checked-in command-registry artifacts with Node-compatible exit behavior for blocked checks. Independent acceptance of the full command registry and production command dispatch remain open. | Rust-owned deterministic generator with byte-compatible outputs, negative tests, and no Node execution dependency. |
| `maintenance/release-attest` | `partial_local_source` | `node` `paper-core/bin/release-evidence.mjs` `--execute` | `hepta-paper-rust release-attest REQUEST` | external authority / target host | Native preflight combines caller-supplied release-state/count projections with local schema-25 freeze and archive inspection and always reports releaseEvidenceReady=false. Native source/provenance capture, release snapshot binding, conformance and policy replay orchestration, signing integration, no-clobber publication and recovery are still implementation gaps. Independent owner/operational acceptance, key custody, exact legacy replay inputs and retirement authority are separate external qualification requirements. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `maintenance/release-integrity-key` | `partial_local_source` | `node` `paper-core/bin/release-integrity-key.mjs` | `hepta-release-integrity-key --action status\|provision [--runtime-root PATH] [--execute]` | external authority / target host | Rust implements status, create-once provisioning, existing-key loading, real Ed25519 PKCS8/SPKI, independent locks, no-clobber publication, identity-bound cleanup and crash refusal. Tested CLI, Node interoperability and error/race matrices pass. Final pair snapshots intentionally reject races accepted by the incumbent. Extended OpenSSL containers, exact malformed-encoding error strings, relocated executable layout and independent command acceptance remain unqualified; local integrity keys do not establish external key custody or release authority. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/advanced-numerical-plugin` | `partial_local_source` | `node` `paper-core/bin/advanced-numerical-plugin.mjs` | `hepta-paper-rust advanced-numerical-plugin REQUEST` | full Rust business call chain | Native Rust now exposes a truthful status projection plus the three checked-in reference-candidate run families (linear-algebra, monte-carlo, optimization). Status reports the exact native families and keeps signed-bundle/runtime-identity/OS-sandbox/independent-evidence/production qualification false. The incumbent signed bundle/trust store, executable/source/package identity, kernel OS sandbox and resource receipt, GPU container authority, complete ten-family surface, independent oracle/replay/uncertainty qualification, provider effects and production activation remain unimplemented; the native result cannot authorize Node retirement. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/automation` | `partial_local_source` | `node` `paper-core/bin/automation-status.mjs` | `hepta-paper-rust automation-status --help [--json]` | external authority / target host | Pure system-status projection (`deriveFullyAutonomousResearchSystemStatus`) now matches a dedicated Node oracle for ready, blocked, and malformed observed inputs. Rust now emits byte-identical deterministic AutomationStatusUsage help metadata for automation-status --help and --json --help and ports the pure JSON readiness evaluator, readiness-level projection, and exit-code policy with Node differential fixtures. The pure evaluator consumes already-observed values only; the command route still does not implement incumbent readiness observers, deployment-environment loading, handoff composition, provider/formal-sandbox/release-attestor effects, store/runtime probes, live authority effects, complete argument modes or independent command acceptance. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/autonomous-empirical-plugin-release` | `partial_local_source` | `node` `paper-core/bin/autonomous-empirical-plugin-release.mjs` | `hepta-paper-rust autonomous-empirical-plugin-release --action template\|plan\|publish\|inspect [--template ABSOLUTE_PATH] [--package-id ID --package-version SEMVER --benchmark-family FAMILY] [--signing-config ABSOLUTE_PATH] [--install-root ABSOLUTE_PATH] [--activation ABSOLUTE_PATH]` | external authority / target host | Rust now provides strict action/template/plan/publish/inspect argument parsing and a bounded no-follow, stable-identity, hash and JSON-shape preflight for template, signing configuration, activation and install-root references. It never loads private keys, executes a signer, verifies an external bundle authority, writes an installation or publishes activation; plan, publish and inspect remain fail-closed pending external authority, immutable installation and target-host acceptance. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/autonomous-intake-authority-rotation` | `partial_local_source` | `node` `paper-core/bin/autonomous-research-machine-intake-authority-rotation.mjs` | `hepta-paper-rust autonomous-intake-authority-rotation --action plan\|apply --runtime-root PATH --next-machine-intake-config PATH --topic-producer-profile PATH [--rotation-intent PATH --expected-authority-generation N --plan-hash sha256:... --execute]` | full Rust business call chain | Rust now provides a bounded no-follow/hash preflight for a v2 machine-intake configuration, topic-producer profile, runtime-root identity and fixed authority-rotation root. Plan and double-confirmed apply remain fail-closed: authority bundle signature verification, SQLite quiescence/lease inspection, target identity conflict checks, external governance, generation CAS, quarantine, journal publication and all mutation remain unimplemented; no external actor or network is contacted. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/autonomous-research` | `partial_local_source` | `node` `paper-core/bin/autonomous-research-readiness.mjs` | `hepta-paper-rust autonomous-research --campaign-id ID (--workflow-file ABSOLUTE_JSON \| --workflow-root ABSOLUTE_STATE --definition-hash SHA256) --action prepare\|launch\|status\|converge\|pause\|resume\|cancel\|amend [--amendment-file ABSOLUTE_JSON] [--through-steps N] [--expected-revision N]` | external authority / target host | Bounded broker manuscript revision now uses the ordinary amendment/converge path with retained CAS manuscript/rejection inputs, an unchanged reviewer contract and query-only uncertain recovery; protocol fixtures are not live independent-role acceptance. The explicit local workflow path does not implement automatic research planning, dynamic trusted request issuance, independent live author/reviewer/revision roles, measured provider-invoice settlement, commit-bound signed ACK, automatic lease renewal, or all incumbent argument modes. Broker transport interruption does not prove provider termination or settle/refund an unknown result. Native in-process work is not preempted mid-calculation. Full role/command acceptance, installed restricted-research qualification, production activation, writer transfer and Node retirement remain unaccepted. Current contracts and runnable coverage are in docs/modules/LOCAL_WORKFLOW_HANDOFF.md; the call chain and named tests below are the machine source for implemented behavior. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/autonomous-research-one-shot-campaign-attempt` | `partial_local_source` | `node` `paper-core/bin/autonomous-research-one-shot-campaign-attempt.mjs` | `hepta-paper-rust autonomous-research-one-shot-campaign-attempt --action plan\|preflight\|execute\|status [--dataset-mount-file PATH\|--attempt-id ID] [--root PATH --runtime-root PATH --control-root PATH]` | external authority / target host | Rust now owns strict plan/preflight/execute/status action parsing, fixed campaign/protected IDs, no-follow single-link dataset mount inspection with bounded content hash, private root checks, and fail-closed side-effect reporting. Execute and status intentionally never open the journal, provider, network, or native writer. Full target campaign inspection, signed dataset authority, provider runtime/canary, external reviewer, journal recovery, and terminal acceptance remain external gaps. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/autonomous-state-backup` | `partial_local_source` | `node` `paper-core/bin/autonomous-research-state-backup.mjs` | `hepta-state-backup --action status\|backup\|restore-drill\|renew\|reconcile-and-renew [--root PATH] [--runtime-root PATH] [--authority-config PATH] [--online-authority-process-config PATH] [--authority-socket-config PATH --authority-socket-config-sha256 sha256:HASH] [--bundle PATH]` | external authority / target host | Complete incumbent CLI/environment compatibility and independent acceptance remain open. General recovery requires the full original effect registry; custom non-heartbeat fallback and reconstruction of callback invocation traces are not supplied. Strict V2 evidence/encoding/SQLite bounds are deliberate compatibility differences, not blanket Node parity. Offline archive/export and supplied-key socket fixtures do not grant live migration, source replacement, production epoch, old-writer retirement or installed key custody. The public owning runtime, qualified installed backup/online authority, retention policy and complete restart/canary/rollback evidence remain required. Implemented behavior is mapped once in callChain/rustSources/testCases; contracts are docs/modules/STATE_BACKUP_CLI_HANDOFF.md, docs/modules/STATE_RECOVERABILITY_HANDOFF.md and docs/modules/LOCAL_STATE_AUTHORITY_HANDOFF.md. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/autonomous-submission-dispatcher` | `partial_local_source` | `node` `paper-core/bin/autonomous-submission-dispatcher.mjs` | `hepta-paper-rust autonomous-submission-dispatcher [--resident] [--campaign-id ID] [--limit N] [--poll-ms MS] [--root PATH] [--runtime-root PATH] [--help]` | external authority / target host | Rust now owns a bounded read-only dispatcher preflight with strict argument parsing, local challenge observation and storage metadata reporting. Resident polling, dispatcher identity and cycle signature verification, portal credential/configuration binding, independent canary authority, handoff SQLite queue inspection, signed cycle publication and external submission delivery remain unported; the native report never authorizes provider action or Node retirement. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/autonomous-submission-dispatcher-challenge` | `partial_local_source` | `node` `paper-core/bin/autonomous-submission-dispatcher-challenge.mjs` | `hepta-paper-rust autonomous-submission-dispatcher-challenge --action status\|publish --plan-hash sha256:... --idempotency-key sha256:... --portal-id ID --portal-configuration-hash sha256:... --portal-descriptor-hash sha256:... [--runtime-root PATH] [--help]` | full Rust business call chain | Rust now owns a bounded read-only challenge-exchange observer: strict arguments, no-follow/nonblocking bounded document reads, exact challenge shape/hash/expiry/binding checks, newest valid challenge selection and fail-closed readiness projection. Publish remains unsupported; dispatcher identity/trust-store signature verification, portal descriptor and independent canary authority, signed cycle envelope, handoff SQLite cutover/queue inspection, resident delivery and external submission effects remain open source work and qualification requirements. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/autonomous-supervisor` | `partial_local_source` | `node` `paper-core/bin/autonomous-research-supervisor.mjs` | `hepta-paper-rust autonomous-supervisor --action health --runtime-root PATH [--require-startup-reconciliation\|--require-machine-intake-reconciliation] [--external-qualification-config PATH] [--help]` | external authority / target host | Rust now exposes the already-qualified read-only supervisor health observer through the unified service CLI, including missing-instance, startup and machine-intake status checks. Shared runtime-root and external-qualification-config options now match Node value-validation precedence and inline syntax; the native action selector follows the same strict grammar. Resident lifecycle execution, signals, instance/cycle leases, campaign scheduling/dispatch, provider canary, qualification renewal, machine-intake enqueue, external-action journal, startup/shutdown recovery and target-host qualification remain open. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/autonomous-supervisor-health` | `partial_local_source` | `node` `paper-core/bin/autonomous-research-supervisor-health.mjs` | `hepta-autonomous-supervisor-health --runtime-root PATH [--require-startup-reconciliation\|--require-machine-intake-reconciliation\|--require-current-machine-intake\|--require-strict-machine-intake-reconciliation\|--require-fully-autonomous] [--external-qualification-config PATH]` | full Rust business call chain | Native health now composes base/startup/machine/current/strict/full modes from actual sources. Full mode observes one intake, actual resident prerequisites and passive state safety; optional strict mode shares the intake but full readiness determines exit. Base health uses a private effective committed-WAL snapshot and does not recreate source SHM. Full environment capture is bounded UTF-8 and source repository selection uses the compiled build tree; relocated installation identity remains unfinished. V2 configuration/admission/producer authority, external plugin registries, local-golden dataset scope, broader legacy Date.parse spellings and publisher/resident execution remain unported. Resident checks are the original narrower signature/time/code/state/configuration/cost contract, not independent full qualification acceptance. Actual V3/recovery trust and KeyObject incompatibilities remain honestly blocked, and no diagnostic grants dispatch/recovery authority. Strict falsy-JSON false-ready behavior is intentionally rejected. Actual CLI, bounded owned fixtures, source preservation, real synthetic signatures and WAL regressions establish local source behavior only; independent principals, production qualification, activation and Node retirement remain external and false. A separately callable native topic-producer profile loader now observes actual profile/dataset/original implementation sources and validates builtin contract/policy identities with retained bounded owners. It is not wired to V2 health: V2 admission and genesis/rotation authority remain open. ICU Unicode/LF ordering is preserved; original backslash alias reads and nonstring optional mount hash transports receive explicit native profile refusals. The observed JS digest is an incumbent diagnostic binding, not accepted Rust execution identity. Separately callable native recorded generation/capability and canary journal/row contracts now reconstruct the original builtin materialized intake, planned hashes, both canary windows, full capability equality, failure accounting and ordered raw action bindings. They perform no SQLite or provider I/O and are not yet actual health call-chain entries. Fabricated recorded canary fixtures establish only pure contract behavior; V2 admission/authority, provider execution and installation remain open. Canonical date/finite JSON profiles remain explicit compatibility limits. Separately callable actual topic status now observes real profile/dataset/implementation inputs, releases their file owners, reads a private effective SQLite/WAL snapshot, validates all history and rebuilds the newest non-NULL capability. Eleven actual-source groups exercise full original diagnostics and explicit finite profile refusals, including real original lease renewal held in WAL and crash/missing-SHM preservation. This composition remains unwired to owning V2 health and proves no provider execution or live authority. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/batch` | `partial_local_source` | `node` `paper-core/bin/paper-production-core.mjs` `batch-run` | `hepta-local-workflow advance STATE HASH ABSOLUTE_THROUGH NOW` | external authority / target host | The bounded local workflow does not implement full incumbent batch/provider scheduling semantics. The copied control-plane inspection diagnostic is not durable whole-plan resource accounting across restart. Dynamic trusted role issuance, independent scientific review, measured provider-invoice settlement, commit-bound signed ACK and installed acceptance remain open. Broker interruption retains the original unresolved identity and permits query-only recovery, not a second execution or refund. Full command parity, production activation and Node retirement remain unaccepted; current behavior is specified in docs/modules/LOCAL_WORKFLOW_HANDOFF.md. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/campaign` | `partial_local_source` | `node` `paper-core/bin/paper-campaign.mjs` | `hepta-local-workflow {list\|status\|events\|logs\|pause\|resume\|amend\|cancel\|cancel-node} ...` | external authority / target host | Bounded broker manuscript revision now uses the ordinary amendment/converge path with retained CAS manuscript/rejection inputs, an unchanged reviewer contract and query-only uncertain recovery; protocol fixtures are not live independent-role acceptance. The sequential local workflow does not implement arbitrary-DAG projection, all incumbent campaign command/effect modes, or authoritative remote/infrastructure cancellation. Preserving a committed step or ambiguous attempt is not settlement of an external result. Dynamic trusted role request issuance, independent scientific review, measured provider cost, commit-bound signed ACK, installed writer/cutover acceptance and Node retirement remain open. See docs/modules/LOCAL_WORKFLOW_HANDOFF.md for current lifecycle and broker interruption/recovery contracts. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/external-authority-intake` | `partial_local_source` | `node` `paper-core/bin/production-external-authority-intake.mjs` | `hepta-paper-rust external-authority-intake [--author-config PATH --author-config-hash sha256:...] [--release-attestor-config PATH --release-attestor-config-hash sha256:...] [--require-ready]` | full Rust business call chain | Rust now matches the covered passive Node author-input cases for missing, malformed, expired, bad-signature, invalid-version, reordered-object, numeric-signature-key, and valid v2 configurations: it performs pinned regular-file identity checks, Node insertion-order JSON shape and configuration-hash verification, subject/policy validation, canonical Ed25519 trust-store and envelope verification, receipt/hash projection, relative path resolution, JavaScript String-coercion signature references, and fail-closed --require-ready behavior. Release-attestor inspection remains a bounded header path; external-kms v3 backend identity, hardware/KMS authority, live signer challenge, external author binding, independent acceptance, production activation, and Node retirement remain blocked behind external authority and target-host evidence. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/full-production-readiness` | `partial_local_source` | `node` `paper-core/bin/full-production-readiness.mjs` | `hepta-paper-rust full-production-readiness --owner-trust-store PATH --owner-trust-store-sha256 sha256:... --owner-acceptance-document PATH --owner-acceptance-document-sha256 sha256:... --package-recovery-readiness-command PATH --package-recovery-readiness-command-sha256 sha256:... [--root PATH] [--runtime-root PATH] [--live-provider-canary] [--live-release-attestor] [--require-full-production]` | external authority / target host | Rust now pins and executes the package readiness helper through a protected descriptor with bounded output, timeout, restricted environment, exact JSON policy and postflight identity checks. Its relative root/runtime arguments and omitted-root sibling defaults now match Node's path.resolve and workspace-layout contract. It validates the exact off-host WORM status protocol but does not probe mounts or custody stores. Native off-host WORM custody and live automation/provider/release-attestor aggregation still require target-host/external evidence; accepted command parity and full production remain blocked. See docs/modules/FULL_PRODUCTION_READINESS_HANDOFF.md. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/gc` | `partial_local_source` | `node` `paper-core/bin/paper-campaign.mjs` `--action` `gc` | `hepta-local-maintenance gc-plan/gc-resume ...` | full Rust business call chain | Local hash-bound garbage collection only; production pin/authentication and Node parity remain unimplemented. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/gc-apply` | `partial_local_source` | `node` `paper-core/bin/paper-campaign.mjs` `--action` `gc` `--apply` | `hepta-local-maintenance gc-apply STATE PLAN HASH` | full Rust business call chain | Local hash-bound garbage collection only; production pin/authentication and Node parity remain unimplemented. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/generic-domain-capability-evidence` | `partial_local_source` | `node` `paper-core/bin/generic-domain-capability-evidence.mjs` | `hepta-paper-rust generic-domain-capability-evidence --action status\|converge --runtime-root ABSOLUTE_PATH` | external authority / target host | Rust now covers a bounded read-only inspector for one explicit canonical runtime evidence file: 16 MiB limit, no-follow private regular-file identity, stable read identity, exact fourteen-key shape and production Node record hash. The status report never returns a production authority decision. The converge route is deliberately fail-closed and never publishes, invokes external replay/Mathlib/reviewer/authority, reads credentials or changes service state. Persisted authority lineage, semantic readiness, formal-domain external replay, independent review, target-host qualification, atomic publication and all incumbent argument/environment modes remain open. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/journal-connector-coverage` | `partial_local_source` | `node` `paper-core/bin/journal-connector-coverage.mjs` | `hepta-journal-connector-coverage [OPTIONS]` | full Rust business call chain | Native discovery for all 98 venues and signed qualification registry inspection/application, pins, freshness, typed evidence, Ed25519/SPKI and organization independence are implemented. Standard Ed25519 SPKI PEM is covered; additional OpenSSL key containers/encodings and full independent command acceptance remain unqualified. Real portal qualification and human live-commit authorization are not supplied by local tests. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/local-golden-dataset-provision` | `partial_local_source` | `node` `paper-core/bin/local-golden-dataset-provision.mjs` | `hepta-paper-rust local-golden-dataset-provision --action plan\|execute [options]` | external authority / target host | Rust now validates a bounded source-bound local-golden plan for the checked-in ml_algorithm_benchmark profile: private runtime/control roots, immutable dataset manifest, complete split assignments, harness cells, semantics, local trust-store scope, time window and plan hashes are inspected without writes. Execute validates the exact plan ID but intentionally returns a blocked report before reading any private key or publishing envelope, trust, mount or receipt outputs. Full all-family contract normalization, Ed25519 authority signing, mount authorization, durable no-clobber publication, runtime envelope verification and independent command acceptance remain open. Exact incumbent repository/workspace/environment protected-root expansion is also not yet ported; this route never grants promotion or external trust. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/nested-runtime-platform-qualification` | `partial_local_source` | `node` `paper-core/bin/nested-runtime-platform-qualification.mjs` | `hepta-nested-runtime-qualification [NODE_OPTIONS] \| --request ABSOLUTE_JSON_PATH` | full Rust business call chain | Native CPU/GPU exact-Pod qualification, startup conformance, independent Ed25519 authority chain, all 18 incumbent flag/environment bindings, help and exit-status behavior are implemented. Canonical clock, public-key encoding, duplicate-key/file identity restrictions and platform I/O error details require compatibility review. Independent command acceptance and real target-host observations remain open. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/personal-gpu-operational-gate` | `partial_local_source` | `node` `paper-core/bin/personal-gpu-operational-gate.mjs` | `hepta-paper-rust personal-gpu-operational-gate --check [--write] [--root PATH] [--runtime-root PATH] [--receipt PATH] [--help]` | full Rust business call chain | Rust covers strict check argument parsing, bounded descriptor-pinned reads, ready/blocked wire verification, Node-compatible output for tested numeric/coercion/property-order cases and private fallback publication only on --check --write failures. Malformed JSON diagnostics, unpaired UTF-16 surrogates and out-of-range JSON numbers now match the Node wire fixtures, including hash verification and fail-closed diagnostics. The complete nvidia-smi/Docker/PDE/deep-learning/CPU-oracle/holdout execute call chain remains source work; real-host qualification, second hardware, external authority, independent acceptance, production activation and Node retirement remain open. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/personal-self-hosted-readiness` | `partial_local_source` | `node` `paper-core/bin/personal-self-hosted-readiness.mjs` | `hepta-paper-rust personal-self-hosted-readiness [--root PATH] [--runtime-root PATH] [--cpu-receipt PATH] [--gpu-enabled --gpu-receipt PATH] [--require-ready] [--now ISO\|UNIX_MILLIS] [--help]` | full Rust business call chain | Rust now composes the read-only personal local observer and matches the incumbent Node report for missing evidence, GPU opt-in failure, and a safe schema-25 database with a valid anti-rollback ledger, content-addressed backup and restore-drill receipt. The route validates the exact formal-operational receipt shape, fixed test inventory, counters, content hash, and ExactCodeProvenance binding with fail-closed mutation tests; it also validates exact provenance, tracked-source secret policy, owner-only runtime boundary, immutable SQLite quick-check/foreign-key/schema/lease state, SQLite-consistent snapshot hashing, anti-rollback entries, backup/restore receipts, and CPU/optional GPU scientific receipt constraints. It never writes runtime state, invokes an external actor, reads private signing material, publishes authority, or enables distribution. Formal runner/TAP receipt production, source-policy parity outside the checked-in tracked scan, hardware execution, external authority, target-host acceptance, deployment and Node retirement remain open. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/portal-target-qualification` | `partial_local_source` | `node` `paper-core/bin/portal-target-qualification.mjs` | `hepta-portal-target-qualification --action status\|preflight\|import-plan\|import-execute [incumbent flags]` | full Rust business call chain | Rust implements all four modes, actual signature/pin/freshness checks, expected target bindings, successor/revocation rules, exact import-plan hashes, and one locked atomic local registry write. Differential fixtures cover complete reports and installed bytes, CLI errors and gates, concurrency, rollback and process-crash refusal. Real independent target qualification packages, extended OpenSSL key encodings, deliberate filesystem safety tightenings and independent command acceptance remain unqualified. No portal request, credential use or live commit permit is provided. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/reconcile` | `partial_local_source` | `node` `paper-core/bin/automation-reconcile.mjs` | `hepta-automation-reconcile [--database ABSOLUTE_SQLITE] [--at ISO_TIME] [--campaign-id ID] [--no-progress-seconds N] [--legacy-terminal-active-residue]` | full Rust business call chain | Complete public writable CLI composition and subsequent-write admission beyond the first exact preimage remain open. Existing V1 cutover-storage migration, broad V8/local-time Date.parse compatibility and independent command acceptance are not complete. The source-private InstalledV2 owner and supplied-key/Node differential fixtures do not qualify the real nine-role installation, continuously bind every handler identity, migrate/retire the running Node authority or authorize production. Historical checkpoint publication, missing-history recovery, installed key/manager custody, target-host canary, data-preserving rollback and old-writer fencing remain required. Local native prepared-cache reconciliation is a narrower path, not online activation. Source and tests are mapped in callChain/rustSources/testCases; contracts are docs/modules/AUTOMATION_RECONCILIATION_EXECUTION_HANDOFF.md, docs/modules/ONLINE_MUTATION_COMPOSITION_HANDOFF.md and docs/modules/LOCAL_STATE_AUTHORITY_HANDOFF.md. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/reconcile-apply` | `partial_local_source` | `node` `paper-core/bin/automation-reconcile.mjs` `--execute` | `hepta-automation-reconcile --execute-local REQUEST_JSON` | full Rust business call chain | The hash-selected local native prepared-result commit is not general online reconciliation or authority to consume broker evidence offline. Public writable production CLI composition, subsequent-write admission beyond the first exact preimage, existing V1 cutover-store migration and broad V8/local-time Date.parse compatibility remain open. Private InstalledV2 composition and differential fixtures do not establish independently qualified installed native adapters, continuous handler identity, live Node-authority migration, production/canary admission or Node retirement. Complete target-host state migration, signed cutover, recovery and data-preserving rollback remain required. Source and tests are mapped in callChain/rustSources/testCases; contracts are docs/modules/AUTOMATION_RECONCILIATION_EXECUTION_HANDOFF.md, docs/modules/ONLINE_MUTATION_COMPOSITION_HANDOFF.md and docs/modules/LOCAL_STATE_AUTHORITY_HANDOFF.md. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/research-capability-matrix` | `partial_local_source` | `node` `paper-core/bin/research-capability-matrix.mjs` | `hepta-paper-rust research-capability-matrix --request ABSOLUTE_JSON_PATH [--require-production-ready]` | full Rust business call chain | Native Rust now exposes the bounded --request CLI over the already-observed descriptive readiness JSON; the optional --require-production-ready flag is a fail-closed diagnostic gate. Pure projection covers all ten capabilities, strict boolean gates, four evidence levels, actual registry inputs, Unicode blocker ordering and recomputed production hashes, with more than 700 real Node cases plus CLI/gate parity. Environment composition, live readiness observations, full incumbent command modes and independent command acceptance remain unimplemented; descriptive JSON never grants authority. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/research-readiness` | `partial_local_source` | `node` `paper-core/bin/automation-status.mjs` `--require-full-research` `--live-provider-canary` `--live-release-attestor` | `hepta-paper-rust research-readiness --workspace-root ABSOLUTE_PATH --runtime-root ABSOLUTE_PATH [--working-directory ABSOLUTE_PATH] [--now UNIX_MILLIS] [--require-ready]` | external authority / target host | The bounded research-readiness CLI now projects the actual Rust state-safety composition and matches the original Node report for the covered passive case, including fail-closed --require-ready behavior and no runtime writes. The composition combines real ten-database inventory, stored backup verification, configured coordinator, complete native source AST coverage and three pinned signatures; full original Node report comparisons include missing/invalid configuration, signed caches, corruption and unregistered writers, with zero authority RPC. General Date.parse coercion/format compatibility, automation-status observers, live provider/attestor flags, complete CLI/environment modes, final runtime activation and independent acceptance remain open. Native stored-source checks deliberately reject some expired or mismatched historical evidence that the Node diagnostic reader accepts; no diagnostic ready field grants activation. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/runtime-image-reproducibility` | `partial_local_source` | `node` `paper-core/bin/runtime-image-reproducibility.mjs` | `hepta-runtime-image-reproducibility --action status\|request\|verify\|publish [--config PATH] [--receipt PATH] [--runtime-root PATH] [--root PATH]` | external authority / target host | Rust implements native status/request/verify/offline-publish, recomputed builtin/dynamic signed plugin scopes, source/input closure, two pinned verifier processes, actual Ed25519 OCI attestations, SQLite monotonic CAS and recoverable derived mirror. Online fenced publication and its activated coordinator/epoch/mirror-permit lifecycle remain unwired; real R source-CAS and two independently qualified rebuild services/evidence remain external. Resource/encoding/filesystem bounds and independent command acceptance remain unqualified. Synthetic signed proofs do not prove real image rebuilds. Native online publication and latest-finalization adapters are now tested as library components, but the real verified activation constructor and CLI integration remain absent. Status-only publication reads now capture actual mirror bytes before private effective-WAL SQLite inspection, retain effective-UID source/sidecar metadata via O_PATH pins and preserve original authority/signature/context results. Source SHM is not created or repaired. Schema/row/cell/VM bounds are explicit native restrictions; the caller must still run before business SQLite. This does not change writer/online-reconciliation lifetimes or close resident composition and independent acceptance gaps. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/runtime-r-source-cas` | `partial_local_source` | `node` `paper-core/bin/runtime-r-source-cas.mjs` | `hepta-paper-rust runtime-r-source-cas REPOSITORY_ROOT [--action status\|acquire] [--seed DIRECTORY]` | external authority / target host | Rust now adds a bounded offline --seed acquisition path with host-tar DESCRIPTION identity checks, exact archive hashes/indexes, private staging, no-replace atomic publication and post-publish verification. Network acquisition, retry/concurrency semantics, seed publication authority, external CRAN transport, tar-tool qualification and target-host qualification remain unimplemented. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/store` | `partial_local_source` | `node` `paper-core/bin/hepta-store.mjs` `status` | `hepta-paper-rust store-status [IMMUTABLE_DB [RUNTIME_ROOT]] [--allow-isolated-verification-evidence]` | full Rust business call chain | Rust now emits the full read-only HeptaNativeStoreStatus projection for an explicitly supplied canonical Node store and also matches Node's omitted-database HEPTA_PAPER_RUNTIME_ROOT discovery and --allow-isolated-verification-evidence mode, including table counts, metadata, receipt/job classifications, contamination gate and submission-handoff cutover diagnostics; production layout discovery beyond the configured runtime root, writable trust/repair/migration authority, and independent acceptance remain unimplemented. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/store-backup` | `partial_local_source` | `node` `paper-core/bin/hepta-store.mjs` `backup` | `hepta-local-maintenance backup STATE DESTINATION` | full Rust business call chain | Local workflow backup primitive only; no production store backup authority or retention qualification. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/store-migrate` | `partial_local_source` | `node` `paper-core/bin/hepta-store.mjs` `migrate` | `hepta-paper-rust store-migrate NODE_DB [TARGET_VERSION]` | full Rust business call chain | Offline local SQLite migration only; target-host writer fencing, production store authority, release evidence, and external qualification remain unimplemented. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/strict-full-auto-acceptance` | `partial_local_source` | `node` `paper-core/bin/strict-full-auto-acceptance.mjs` | `hepta-paper-rust strict-full-auto-acceptance --action plan\|inspect-runtime-adoption-candidate\|adoption-status\|adopt-runtime\|status\|execute\|converge --configuration ABSOLUTE_PATH [--plan-hash sha256:... --execute] [--require-accepted\|--require-adopted]` | external authority / target host | Rust now matches the strict action/confirmation parser and performs a bounded no-follow configuration reference preflight. It never creates the immutable plan/lease, verifies all 30 external references and 15 steps, inspects or adopts a pristine runtime, executes child commands, persists checkpoints, performs live verification, or claims strict acceptance; all external authority and target-host evidence remain blocked. | Complete Rust call chain first, then target-host/external authority qualification, recovery evidence, and writer/credential ownership. |
| `operator/submission-handoff` | `partial_local_source` | `node` `paper-core/bin/paper-submission-handoff.mjs` | `hepta-native-business CAP-SUBMIT / NativeBusinessJobV1::PrepareSubmission [deterministic package preparation only]` | full Rust business call chain | Rust natively validates and deterministically prepares a bounded non-authoritative submission package, with artifact references and hashes. The Node handoff still owns current campaign-release authority query, release/promotion/verification receipt binding, artifact existence/content verification, provider/portal account qualification, and handoff-output compatibility; no submission is dispatched or authorized by the Rust kernel. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/submission-handoff-export` | `partial_local_source` | `node` `paper-core/bin/paper-submission-handoff-export.mjs` | `hepta-paper-rust submission-handoff-export --action inspect\|export --campaign-id ID --bundle-root ABSOLUTE_PATH --request ABSOLUTE_JSON_PATH [--root ABSOLUTE_PATH] [--runtime-root ABSOLUTE_PATH]` | full Rust business call chain | Rust now provides a bounded read-only request/layout preflight with nofollow regular-file identity checks, stable parent/file metadata, exact top-level request shape, Node paper-record hash recomputation (including manifest hash alias), nested kind/status/safety/hash/binding and decision/preflight/dispatch contract checks, campaign binding, and explicit no-mutation/no-provider semantics. Full Node parity still requires persisted authority lineage verification, consuming the current verified release/submission authority, crash-safe bundle publication/recovery, broad JavaScript Date/number/wire coercion compatibility, and independent acceptance; export action remains fail-closed and never publishes. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `operator/workspace` | `partial_local_source` | `node` `paper-core/bin/workspace-status.mjs` | `hepta-workspace-status [--workspace-root PATH] [--require-decoupled]` | full Rust business call chain | Native read-only workspace-root, asset/runtime/legacy layout and physical-decoupling diagnostics match the Node oracle for the UTF-8 relocatable profile, including missing-suffix symlink resolution, hop limits, overlap blockers, and the incumbent compiled deployment-root default. Native CLI relocation remains covered separately; independent command acceptance remains open. | Complete Rust call chain covering every argument mode, state transition, lease, retry, crash, cancellation, and external-effect boundary. |
| `retirement/drill` | `partial_local_source` | `node` `paper-core/bin/legacy-deletion-drill.mjs` | `cargo run -p hepta-cutover --example local_cutover_drill -- ABSENT_DIRECTORY` | verification / retirement authority | Local cutover drill only; protected production retirement evidence remains unimplemented. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `retirement/drill-attest` | `partial_local_source` | `node` `paper-core/bin/legacy-deletion-drill.mjs` `--attest` | `hepta-paper-rust retirement-drill-attest REQUEST` | verification / retirement authority | Rust now performs a native immutable schema-25 freeze and archive identity inspection and emits a self-hashed blocked report. It does not run the incumbent isolated Node p0/p1 or matrix replay, independently verify release provenance, obtain owner or operational acceptance, use the release signing key, publish a receipt, or authorize physical deletion; those independent qualification gates remain external. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `retirement/matrix` | `partial_local_source` | `npm` `run` `migration:capability-matrix-v3` | `hepta-paper-rust retirement-matrix --workspace-root ABSOLUTE_PATH --runtime-root ABSOLUTE_PATH` | verification / retirement authority | Native Rust performs a bounded, read-only projection of all explicit-retirement rows, conservative decision classification, source/target hash inspection and owner-status import. It never executes the Node release profile, verifies capability conformance or production receipts, obtains external owner acceptance, replays the private legacy archive, or grants retirement authority; blocked output exits non-zero. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `retirement/reference` | `partial_local_source` | `node` `migration/bin/verify-retirement-source-snapshot.mjs` | `hepta-paper-rust retirement-reference ROOT` | verification / retirement authority | Rust verifies receipt-bound archive bytes and hashes with the Node oracle; exact target reference root, immutable filesystem attributes, historical replay and independent retirement authority remain external qualification work. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `retirement/status` | `partial_local_source` | `npm` `run` `migration:retirement-status` | `hepta-retire-legacy-archive [status\|--help\|-h\|--execute]; hepta-paper-rust retirement-status [REQUEST]` | verification / retirement authority | Rust matches the pure read-only legacy archive status contract for explicit roots and version, Node package-version plus workspace/env default resolution, and the standalone incumbent CLI default/status/help/argument-error/blocked-execute JSON and exit-code contract. The route remains strictly read-only; destructive identity-bound execution, exact retirement authority and independent acceptance remain unimplemented. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `verify/architecture` | `partial_local_source` | `node` `--test` `paper-core/tests/architecture-conformance.test.mjs` | `hepta-paper-rust verify-architecture ROOT [--json] [--strict]` | verification / retirement authority | Rust now performs a read-only source-boundary/import-graph inspection for every declared architecture manifest category, retired workflow path, production/reference boundary, domain filesystem/clock rule, compatibility manifest, and research/dispatcher network boundary. It does not execute the complete Node architecture-conformance runtime assertions, provider/artifact/SQLite/formal-verifier behavior, argument/error matrix, or independent acceptance; the native report never grants production activation or Node retirement. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `verify/critical` | `partial_local_source` | `npm` `run` `coverage:critical-modules` | `hepta-paper-rust verify-critical [--root ABSOLUTE_PATH] [--runtime-root ABSOLUTE_PATH] [--evidence ABSOLUTE_JSON_PATH --evidence-sha256 sha256:...] [--require-ok] [--json]` | verification / retirement authority | Rust now performs a bounded read-only inventory of the checked-in critical-module target registry, matches the Node compiled workspace root and HEPTA_PAPER_RUNTIME_ROOT/sibling native-runtime defaults, and validates an explicitly pinned Node CriticalModuleCoverageReport shape/hash. It does not execute the isolated Node coverage/test batch, verify a cryptographic signature or external authority, recompute V8 coverage, or grant critical coverage parity; --require-ok remains fail-closed. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `verify/full` | `partial_local_source` | `npm` `test` | `hepta-paper-rust verify-full --workspace-root ABSOLUTE_PATH [--require-parity] [--json]` | local parity acceptance | Rust now performs a read-only inventory of the checked-in Node npm test manifest and test-source files plus Rust workspace provenance/counts. It never executes Node or npm, does not compare test outcomes or failure matrices, and cannot independently accept full-suite parity or retire Node; --require-parity remains fail-closed with exit code 2. | Rust implementation of the complete Node test surface, argument modes, failure matrix, and independent acceptance evidence. |
| `verify/operational` | `partial_local_source` | `node` `paper-core/bin/operational-proof-status.mjs` | `hepta-operational-proof-status [--workspace-root ABSOLUTE_PATH] [--runtime-root ABSOLUTE_PATH] [--asset-root ABSOLUTE_PATH]` | verification / retirement authority | Native Ed25519 verification, all 16 capabilities, source-bound replay validation and sealed hydrated-submodule closure are implemented. The standalone CLI now matches the incumbent workspace/runtime/asset environment defaults while preserving explicit absolute-root overrides; independent complete-mode acceptance and real external operational evidence remain open. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `verify/owner` | `partial_local_source` | `node` `paper-core/bin/owner-acceptance-status.mjs` | `hepta-owner-acceptance-status [--workspace-root ABSOLUTE_PATH] [--runtime-root ABSOLUTE_PATH]` | verification / retirement authority | Native current-matrix family rebuilding and v1/v2 signed owner acceptance status match Node fixtures. The standalone CLI now matches the incumbent workspace/runtime environment defaults while preserving explicit absolute-root overrides; stronger no-alias input restrictions require compatibility review, and real independent owner acceptance and retirement authority remain absent. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `verify/release` | `partial_local_source` | `npm` `run` `release:verify` | `hepta-paper-rust release-state REQUEST` | verification / retirement authority | Rust matches the pure Node package/document/tag consistency contract, including omitted tag snapshots, duplicate-tag diagnostics, newer-tag deduplication, and finite JavaScript Number rounding/stringification at the JSON boundary. Extremely large JSON numbers that Node parses as Infinity remain fail-closed because the native bounded parser rejects them; npm security audit, complete release verification suite, target-host release authority and production publication remain unimplemented. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `verify/repository-assets` | `partial_local_source` | `node` `paper-core/bin/repository-asset-status.mjs` | `hepta-paper-rust repository-assets [ROOT MANIFEST] [--handoff] [--require-externalized]` | verification / retirement authority | Rust ports the read-only repository asset identity, external-reference, restore-receipt and gitlink checks, including the incumbent optional-transport guard, strict duplicate/unknown/boolean argument handling, default blocked-boundary exit status, and --require-externalized output/exit behavior, including handoff combinations. Omitted ROOT/MANIFEST arguments now resolve a canonical manifest from the current deployment root, with a compiled source-root fallback; explicit paths remain available for isolated deployments. Independent-root CLI fixtures execute the original Node entrypoint against ready, pending and invalid local identities. Independent release authority remains unimplemented. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `verify/store` | `partial_local_source` | `node` `paper-core/bin/hepta-store-logical-integrity.mjs` | `hepta-paper-rust store-integrity [IMMUTABLE_DB]` | verification / retirement authority | Rust now emits the complete read-only logical-integrity report with byte pre/post hashes, schema/table hashes, quick_check, foreign-key count, receipt-ledger hash validation, bounded blockers and fail-closed status; 25 generated Node migration stores match the report. The CLI also matches Node's implicit HEPTA_PAPER_RUNTIME_ROOT/hepta-paper.sqlite default and explicit-path form through a real Node/Rust fixture. Production store discovery, trust/repair/migration authority, historical recovery, external qualification and independent acceptance remain unimplemented. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |
| `verify/trust` | `partial_local_source` | `node` `paper-core/bin/release-trust-gate.mjs` | `hepta-paper-rust release-trust-gate REQUEST` | verification / retirement authority | Rust matches the covered pure Node release trust-layer gate and its Node 22 record hash, including JavaScript String(value \|\| '') release-commit coercion, ECMAScript whitespace/BOM handling, array/object ToPrimitive cases, JavaScript number wire formatting across exponent thresholds, and 0b/0o numeric count forms, and preserves Node error precedence when multiple count inputs are invalid. The count adapter remains intentionally bounded and fail-closed for arbitrary JavaScript Number edge cases outside the tested finite integral profile; runtime capability-proof loading, external owner signatures, release authority and production activation remain unimplemented and must stay fail-closed. | Rust verifier/retirement implementation plus exact-head, historical, recovery, maintainer evaluation, and authority-removal evidence. |

Partial source candidates remain in this ledger with their remaining implementation and qualification gaps. A smaller unmapped count is source-mapping progress, not closure. This source-only generator cannot remove a route as accepted: independent command/mode acceptance and the required qualification evidence need a separate verified acceptance process.
