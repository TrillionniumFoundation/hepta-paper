# 旧 qualification-evidence 分支核对

旧 all-gap ref `fab2458e98c4021baf194ae86155f95e3c3c9b3f` 与当前 `c5c08fbe3c1e469171126ba09c4c47c6d48a63c4`；[文件哈希](hepta-qualification-evidence-branch-review-20260922.json)。本轮只读，没有编译或运行旧测试。

旧 crate 的两个源文件存在，但不在该 ref 的 workspace.members 中；在库外 Rust 搜索未发现 hepta-qualification-evidence / verify_external_evidence / ChallengeLedgerV1 调用。内部 CLI 11个参数验证单个 evidence，再调用 SQLite ChallengeLedgerV1::consume，两个同文件单测包含签名、附件与重复挑战场景；其存在不证明 workspace 构建或生产接入。

旧 envelope 包含 challenge、subject、issuer、claims、attachments 和 signature，签名 domain 是 HeptaExternalQualificationEvidenceV1；当前 seven-package ExternalQualificationEnvelopeV1/opaque closure 是不同协议，不能重命名或混用签名字节。旧 VerifiedExternalEvidenceV1 字段公开；当前完整 closure 的验证身份是私有不可反序列化类型。当前 public 单package candidate同样不构成完整生产许可。

旧 ledger 的 open 在核验 application_id/user_version 前先写这两项并 CREATE TABLE IF NOT EXISTS；不能直接移植为当前安全恢复路径。旧 trust document仅derive Deserialize，但 from_json_bytes传入要求Serialize的canonical_json_bytes；本轮没有编译，明确这是静态发现的缺失trait疑点，不宣称某次构建失败。

当前实际 CLI hepta-qualification-closure 调用真实七套 envelope/payload 的联合 factory，并在一个 V2 ledger transaction 检查/推进time、trust generation、七个nonce和receipt。精确重复返回原canonical报告，部分重复/冲突拒绝；schema完整核验、文件所有权与预算见已提交HANDOFF。旧单challenge“一律拒绝重复”和新完整集合“精确重复幂等”不是相同接口契约；本报告不授权旧部署自动迁移，也不重跑现有测试。

仍存在的明确边界：CLI在读取前采样一次wall clock，后续输入读取及SQLite锁等待不会重取当前时间；报告是该采样时刻的non-activating接受。底层显式时间factory/内存opaque closure不能替代实际安装、持续撤销、独立签名主机事实或原Node退役。该原有时间边界已在HANDOFF公开，后续可以单独改进真实CLI的验收时点；不能从旧文件存在推断这个gap已闭合。
