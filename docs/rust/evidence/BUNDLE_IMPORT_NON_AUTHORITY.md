# Bundle import non-authority

Importing a source bundle is a development transport only. It neither qualifies the imported source nor elevates any evidence tier. The resulting exact Git tree must pass the protected validation matrix independently.
