# Exact tree rule

Every source qualification record must bind both the pull-request head commit and its Git tree. Testing a synthetic merge ref, an earlier head, or a later documentation-only commit cannot qualify the current tree unless the exact-tree relation is proven and recorded.
