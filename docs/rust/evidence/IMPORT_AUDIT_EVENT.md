# Import audit event

Every accepted development source import must record source bundle hash, manifest hash, parent commit, resulting commit/tree, workflow identity and validation run IDs. These records are development provenance, not production authority.
