# Production non-authorization

The unified gap-closure branch may implement and source-qualify code. It must not enable real provider credentials, activate a production campaign writer, sign or publish a release, access KMS/HSM/WORM credentials, mutate a portal, or submit a paper. Those state transitions require the separately accepted evidence identified by Plan v3.
