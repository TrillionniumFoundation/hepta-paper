# Final Rust replacement exact-head request

This record requests a fresh, complete, non-empty qualification matrix for the current direct-to-main release-candidate head.

It does not assert success. Admission requires the current exact commit and tree to pass all repository, Rust, Node-compatibility, documentation, supply-chain, program-truth, source-snapshot, migration, broker, qualification-subject, and effective-currentness workflows without skipped required jobs.

The candidate must retain all of these properties:

- native Rust production composition only;
- no Node or generic shell bridge accepted as a native production capability;
- actual immutable Node database drain/freeze proof before the first Rust writer lease;
- deterministic Rust snapshot, routing, hierarchical resource, telemetry, and performance contracts;
- exact Node schema/hash compatibility and reversible state migration;
- fail-closed production activation unless all independently controlled external receipts are current;
- no source, hosted-runner, administrator, fixture, or self-signed substitution for target-host, private replay, credential, key, release, submission, independent-review, or actual retirement evidence.

Any head, base, workflow, generated merge, policy, review, or evidence movement invalidates the requested result and requires a new exact-head matrix.
