# Qualification Subject V3 final matrix trigger

This final source-only marker intentionally changes `rust/**` after all V3
collector, schema, derivation, revalidation, workflow, and canonical-policy files
have been written. It forces the complete manifest-bound producer set to execute
on one frozen pull-request head.

It grants no production, provider, writer, target-host, key, release, immutable
storage, portal, submission, or external-authority permission.
